# GlassPrint runtime dependencies
#
# PySide6 provides the Qt Quick UI, QtPdf (PDFium) for PDF rasterisation and
# QtMultimedia for the sound effects. Pillow handles every raster image format
# and, crucially, the DIB blit onto the Windows printer device context.
# pywin32 is the whole printer capability / spooler layer and is Windows only,
# so the app degrades to a simulation backend on other platforms.

PySide6==6.11.1
Pillow==12.3.0
pywin32>=306; sys_platform == "win32"
